it exports and imports fine without error, I can even see the title of the note after importing to another phone (using same password) but the contents sais
E009: Decryption failed - error:1e000065:Cipher functions:OPENSSL_internal:BAD_DECRYPT
instead of the actual content!

I like how it combines the notes upon import (it dont overwrite already stored data this is good)

Fix the contents encryption / decrytption issue when exporting / importing

In about section it should say what github app name version its using so that in the future it can detect if new version available and show an update button that takes user to the releases page to get the newest version! (cant really show update buttoppn if it dont even show current version)

impliment the check if you can using the github link to this app apk)

dont edit this file

save changes to changes.txt (create if not exists)

